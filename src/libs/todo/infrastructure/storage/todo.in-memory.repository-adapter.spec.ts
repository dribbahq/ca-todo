describe("TodoInMemoryRepositoryAdapter", () => {
  beforeEach(() => {
    jest.resetAllMocks();
  });

  describe("methods", () => {
    describe("findById", () => {
      it("should throw an error when repository adapter fails", () => {
        // TODO:
      });

      it("should return the TODO entity", () => {
        // TODO:
      });
    });

    describe("save", () => {
      it("should throw an error when repository adapter fails", () => {
        // TODO:
      });

      it("should save the TODO entity", () => {
        // TODO:
      });
    });
    describe("delete", () => {
      it("should throw an error when repository adapter fails", () => {
        // TODO:
      });

      it("should delete the TODO entity", () => {
        // TODO:
      });
    });
  });
});
